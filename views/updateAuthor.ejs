<!DOCTYPE html>
<html>
    <head>
        <title> </title>
    </head>
    <body>

        <h1> Updating Author </h1>
        

        <form method="POST">
        
            First name: <input type="text" name="firstName"
                        value="<%= authorInfo.firstName %>"><br>    
            Last  name: <input type="text" name="lastName"
                        value="<%= authorInfo.lastName %>"><br>    
            Gender: <input type="radio" name="gender" value="F" 
                         <%= authorInfo.sex == 'F'?"checked":"" %>
                        >Female
                    <input type="radio" name="gender" value="M"
                       <%= authorInfo.sex == 'M'?"checked":"" %>
                    >Male
                    
            <input type="hidden" name="authorId" value="<%= authorInfo.authorId %>"> 
            
            <!--TODO: Add the remaining fields -->
             DOB: <input type="date" name="dob"
                        value="<%= authorInfo.dob %>"><br> 
             DOD: <input type="date" name="dod"
                        value="<%= authorInfo.dod %>"><br> 
            Profession: <input type="text" name="profession"
                            value="<%= authorInfo.profession %>"><br>    
            Country: <input type="text" name="country"
                        value="<%= authorInfo.country %>"><br> 
            <br>
            Biography: <textarea name="biography" cols="50" height="20"
                    value="<%= authorInfo.biography %>"></textarea>
            <br><br>
            
            <br><br>
            <button>Update Author</button>
        
            <%= typeof(message) != "undefined"?message:"" %>
            
        </form>        

        
    </body>
</html>